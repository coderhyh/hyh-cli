import { Context, Next } from 'koa'

import errorTypes from '~/constants/error-types'

class <%= bigHumpName %>Middleware {
  async def(ctx: Context, next: Next) {}
}

export const { def } = new <%= bigHumpName %>Middleware()
